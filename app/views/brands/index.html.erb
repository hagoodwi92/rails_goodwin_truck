<% if @brands.any? %>
<table class="table table-striped">
  <tr>
    <th scope="col"><h1>Brands</h1></th>
  </tr>
  
<% @brands.each do |brand| %>
  <tr>
    <td><%= link_to brand.name, brand_path(brand) %></td></br>
  </tr>
  <% end %>
    <% else %>
    <p>There are no brands yet.</p>
    <% end %>
    </br>

    </table>
</br></br>
  <p><%= link_to "Create new brand", new_brand_path %></p>